---
inventory_layout:
  vars:
    hostname: __IP__
  hosts:
    cinch-group:
      count: 1
      host_groups:
        - rhel7
        - certificate_authority
        - repositories
        - jenkins_slave
